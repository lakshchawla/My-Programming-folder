#include <stdio.h>
#include <stdlib.h>
int main(void)
{
    int lim;
    printf("Enter the number of elements you want to enter: "); scanf("%d",&lim);

    int *ptr;
    
    ptr = (int*)malloc(lim * sizeof(int));


}

        