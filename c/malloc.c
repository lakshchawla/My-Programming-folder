#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include<ctype.h>

int main(void)
{
    // use of malloc
    int *ptr, n = 3;
    ptr = (int *)malloc(n*sizeof(int));

    for (int i = 0; i < n; i++)
    {
        printf("Enter value of ")
    }
    
    
    return 0;
}

